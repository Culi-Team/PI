<UserControl x:Class="PIFilmAutoDetachCleanMC.MVVM.Views.FooterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PIFilmAutoDetachCleanMC.MVVM.Views"
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:PIFilmAutoDetachCleanMC.MVVM.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:FooterViewModel}"
             d:DesignHeight="70" d:DesignWidth="800">
    <DockPanel Background="{StaticResource PrimaryBackgroundColor}">
        <Grid Height="2.5"
              DockPanel.Dock="Bottom"/>
        <Grid Height="2.5"
              DockPanel.Dock="Top"/>
        <ContentControl Content="{Binding NavigateVM}"
                        Visibility="{Binding IsInitializing, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Height="75"/>
    </DockPanel>
</UserControl>
