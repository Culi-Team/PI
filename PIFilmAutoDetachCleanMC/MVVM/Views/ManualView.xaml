<UserControl x:Class="PIFilmAutoDetachCleanMC.MVVM.Views.ManualView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PIFilmAutoDetachCleanMC.MVVM.Views"
             xmlns:vm="clr-namespace:PIFilmAutoDetachCleanMC.MVVM.ViewModels"
             xmlns:uc="clr-namespace:EQX.UI.Controls;assembly=EQX.UI"
             d:DataContext="{d:DesignInstance Type=vm:ManualViewModel}"
             mc:Ignorable="d"
             x:Name="root"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <!-- TODO: Use ItemsControl? -->
        <ContentControl Content="{Binding Inputs.All[0]}"/>
        <ContentControl Content="{Binding Inputs.All[1]}"/>
        <ContentControl Content="{Binding Inputs.All[2]}"/>

        <ContentControl Content="{Binding Outputs.All[0]}"/>
        <ContentControl Content="{Binding Outputs.All[1]}"/>
        <ContentControl Content="{Binding Outputs.All[2]}"/>

        <uc:MotionView Motion="{Binding DataContext.Motions.X1Axis,ElementName=root}"/>

        <Button Content="Connect Motion" Command="{Binding ConnectMotionCommand}"/>
    </StackPanel>
</UserControl>
