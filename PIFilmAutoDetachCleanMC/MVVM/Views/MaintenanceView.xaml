<UserControl x:Class="PIFilmAutoDetachCleanMC.MVVM.Views.MaintenanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PIFilmAutoDetachCleanMC.MVVM.Views"
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:PIFilmAutoDetachCleanMC.MVVM.ViewModels"
             d:DataContext="{d:DesignInstance Type={x:Type vm:MaintenanceViewModel}}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Button
            Margin="0 0 0 50"
            DockPanel.Dock="Bottom"
            Content="SET ACTIVE SCREEN"
            Width="300" Height="50"
            VerticalAlignment="Bottom"
            Command="{Binding SwitchActiveScreen}"/>
        
        <Image Source="{StaticResource PM}"
               Stretch="None"/>
    </Grid>
</UserControl>
