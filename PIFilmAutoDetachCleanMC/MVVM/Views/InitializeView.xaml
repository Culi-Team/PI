<UserControl x:Class="PIFilmAutoDetachCleanMC.MVVM.Views.InitializeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PIFilmAutoDetachCleanMC.MVVM.Views"
             xmlns:vm="clr-namespace:PIFilmAutoDetachCleanMC.MVVM.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:InitializeViewModel}"
             d:DesignHeight="600" d:DesignWidth="1000">
    
    <UserControl.Resources>
        <Style x:Key="ModuleToggleStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="#33000000"
                                BorderBrush="Transparent"
                                CornerRadius="8"
                                Padding="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#AA1E88E5" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#FFE3F2FD" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#661E88E5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Margin" Value="0 6" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Height" Value="48" />
        </Style>

        <Style x:Key="ModuleActionButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#00897B" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FontSize" Value="8" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsInitializing}" Value="True">
                    <Setter Property="Background" Value="#FF8F00" />
                </DataTrigger>
                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                    <Setter Property="Background" Value="#2E7D32" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="240"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Margin="0 0 12 0"
                Background="White"
                CornerRadius="12">
            <Viewbox Stretch="Uniform">
                <Grid Width="1287" Height="366">
                    <Image Source="{StaticResource AutoViewImage}"
                           Stretch="Fill"
                           Opacity="1"/>
                    <ItemsControl ItemsSource="{Binding Modules}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding PositionX}" />
                                <Setter Property="Canvas.Top" Value="{Binding PositionY}" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border x:Name="ModuleBorder"
                                        Width="{Binding Width}"
                                        Height="{Binding Height}"
                                        Background="#66001B44"
                                        BorderBrush="#66FFFFFF"
                                        BorderThickness="1.5"
                                        CornerRadius="14"
                                        Padding="10"
                                        ToolTip="{Binding ProcessSummary}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <ToggleButton Grid.Row="0"
                                                      Style="{StaticResource ModuleToggleStyle}"
                                                      Content="{Binding DisplayName}"
                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"/>
                                        <TextBlock Grid.Row="1"
                                                   Text="{Binding StepNext}"
                                                   Foreground="White"
                                                   FontSize="13"
                                                   HorizontalAlignment="Center"
                                                   Margin="0,10,0,6"/>
                                        <Button Grid.Column="2"
                                                Content="{Binding ButtonContent}"
                                                Command="{Binding InitializeCommand}"
                                                Style="{StaticResource ModuleActionButtonStyle}"/>
                                        <TextBlock Grid.Row="3"
                                                   Text="Initializing...."
                                                   Foreground="White"
                                                   FontSize="12"
                                                   Margin="0,6,0,0"
                                                   HorizontalAlignment="Center"
                                                   Visibility="{Binding IsInitializing, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                    </Grid>
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                        <Setter TargetName="ModuleBorder" Property="Background" Value="#992157BD" />
                                        <Setter TargetName="ModuleBorder" Property="BorderBrush" Value="#FFE0F2F1" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsInitializing}" Value="True">
                                        <Setter TargetName="ModuleBorder" Property="BorderBrush" Value="#FFFFF59D" />
                                        <Setter TargetName="ModuleBorder" Property="BorderThickness" Value="2.5" />
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Viewbox>
        </Border>

        <StackPanel Grid.Column="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center">
            <TextBlock Text="PIDetach Modules"
                       FontWeight="Bold"
                       FontSize="18"
                       Foreground="#212121"
                       Margin="0 0 0 10" />
            <Button Content="Select All"
                    Command="{Binding SelectAllCommand}"
                    Background="#FFD54F" />
            <Button Content="Unselect All"
                    Command="{Binding UnselectAllCommand}"
                    Background="#E0E0E0" />
            <Button Content="Initialize"
                    Command="{Binding InitializeCommand}"
                    Background="#00C853"
                    Foreground="White"
                    Height="64"
                    FontSize="18" />
            <Button Content="Back"
                    Command="{Binding NavigateBackCommand}"
                    Background="#BDBDBD" />
            <Border BorderBrush="#DDDDDD"
                    BorderThickness="1"
                    Margin="0 12 0 0"
                    Padding="8"
                    CornerRadius="6"
                    Background="#F5F5F5">
                <TextBlock Text="{Binding StatusMessage}"
                           TextWrapping="Wrap"
                           Foreground="#333333"
                           FontSize="14" />
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
