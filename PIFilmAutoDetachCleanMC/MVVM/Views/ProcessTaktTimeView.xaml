<UserControl x:Class="PIFilmAutoDetachCleanMC.MVVM.Views.ProcessTaktTimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/PIFilmAutoDetachCleanMC;component/Styles/TaktTimeStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

        <!-- Process Item DataTemplate -->
        <DataTemplate x:Key="ProcessItemTemplate">
            <Border Style="{StaticResource CellBorderStyle}" Margin="0,2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Rectangle Grid.Column="0" Width="1" Fill="LightGray" HorizontalAlignment="Right" Margin="0,5"/>
                    <Rectangle Grid.Column="1" Width="1" Fill="LightGray" HorizontalAlignment="Right" Margin="0,5"/>
                    <Rectangle Grid.Column="2" Width="1" Fill="LightGray" HorizontalAlignment="Right" Margin="0,5"/>
                    <Rectangle Grid.Column="3" Width="1" Fill="LightGray" HorizontalAlignment="Right" Margin="0,5"/>
                    
                    <TextBlock Grid.Column="0" Text="{Binding ProcessName}" Style="{StaticResource LabelTextStyle}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="{Binding LastTime, StringFormat='{}{0:F2} s'}" Style="{StaticResource ValueTextStyle}"/>
                    <TextBlock Grid.Column="2" Text="{Binding CurrentTime, StringFormat='{}{0:F2} s'}" Style="{StaticResource ValueTextStyle}"/>
                    <TextBlock Grid.Column="3" Text="{Binding AverageTime, StringFormat='{}{0:F2} s'}" Style="{StaticResource ValueTextStyle}"/>
                    <TextBlock Grid.Column="4" Text="{Binding TotalTime, StringFormat='{}{0:F2} s'}" Style="{StaticResource ValueTextStyle}"/>
                </Grid>
            </Border>
        </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FF2E3440" Padding="15" MouseLeftButtonDown="Header_MouseLeftButtonDown">
            <StackPanel>
                <TextBlock Text="Process TaktTime Monitor" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Foreground="White" 
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Real-time monitoring of all process TaktTimes" 
                           FontSize="12" 
                           Foreground="LightGray" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Process Table -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="10">
            <StackPanel Orientation="Vertical">

                <Border Style="{StaticResource HeaderBorderStyle}" Margin="0,2">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!--header -->
                        <Rectangle Grid.Column="0" Width="1" Fill="White" HorizontalAlignment="Right" Margin="0,5"/>
                        <Rectangle Grid.Column="1" Width="1" Fill="White" HorizontalAlignment="Right" Margin="0,5"/>
                        <Rectangle Grid.Column="2" Width="1" Fill="White" HorizontalAlignment="Right" Margin="0,5"/>
                        <Rectangle Grid.Column="3" Width="1" Fill="White" HorizontalAlignment="Right" Margin="0,5"/>
                        
                        <TextBlock Grid.Column="0" Text="Process Name" Style="{StaticResource HeaderTextStyle}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="Last Time" Style="{StaticResource HeaderTextStyle}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="Current Time" Style="{StaticResource HeaderTextStyle}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="3" Text="Average Time" Style="{StaticResource HeaderTextStyle}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="4" Text="Total Time" Style="{StaticResource HeaderTextStyle}" VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- ItemsControl -->
                <ItemsControl ItemsSource="{Binding ProcessTaktTimeItems}" 
                              ItemTemplate="{StaticResource ProcessItemTemplate}"/>

            </StackPanel>
        </ScrollViewer>
        
        <!-- Buttons -->
        <Grid Grid.Row="2" Margin="10,5,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="0" 
                    Content="Reset TaktTime" 
                    Width="100"
                    Height="40"
                    HorizontalAlignment="Left"
                    Command="{Binding ResetTaktTimeAllCommand}"/>
            <Button Grid.Column="1" 
                    Content="Close" 
                    Width="100"
                    Height="40"
                    HorizontalAlignment="Right"
                    Click="CloseButton_Click"/>
        </Grid>
    </Grid>
</UserControl>